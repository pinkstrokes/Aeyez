services:
  aeyez:
    build: .
    image: aeyez:latest
    ports:
      - "8000:8000"
    environment:
      AEYEZ_ENV: prod
      # JWT_SECRET is REQUIRED — compose refuses to start without it.
      JWT_SECRET: ${JWT_SECRET:?set JWT_SECRET in .env or your shell}
      # OPENAI_API_KEY is required for live Spaz model responses.
      OPENAI_API_KEY: ${OPENAI_API_KEY:?set OPENAI_API_KEY in .env or your shell}
      SEEINGEYE_TRANSLATOR_BASE_URL: ${SEEINGEYE_TRANSLATOR_BASE_URL:-https://api.openai.com/v1}
      SEEINGEYE_REASONER_BASE_URL: ${SEEINGEYE_REASONER_BASE_URL:-https://api.openai.com/v1}
      SEEINGEYE_TRANSLATOR_MODEL: ${SEEINGEYE_TRANSLATOR_MODEL:-gpt-5.4-mini}
      SEEINGEYE_TRANSLATOR_ESCALATION_MODEL: ${SEEINGEYE_TRANSLATOR_ESCALATION_MODEL:-gpt-5.4-mini}
      SEEINGEYE_REASONER_MODEL: ${SEEINGEYE_REASONER_MODEL:-gpt-5.4-mini}
      SEEINGEYE_VIDEO_FRAME_INTERVAL_S: ${SEEINGEYE_VIDEO_FRAME_INTERVAL_S:-0.5}
      SEEINGEYE_VIDEO_MAX_FRAMES: ${SEEINGEYE_VIDEO_MAX_FRAMES:-64}
      # Optional integrations.
      ELEVENLABS_API_KEY: ${ELEVENLABS_API_KEY:-}
      ELEVENLABS_VOICE_ID: ${ELEVENLABS_VOICE_ID:-}
      GOOGLE_MAPS_API_KEY: ${GOOGLE_MAPS_API_KEY:-}
    volumes:
      - aeyez-data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/health').status==200 else 1)"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── Optional reverse proxy for TLS termination on a public deploy ─────────
  # Uncomment + configure a Caddyfile to serve at https://your.domain
  # caddy:
  #   image: caddy:2-alpine
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./Caddyfile:/etc/caddy/Caddyfile:ro
  #     - caddy-data:/data
  #     - caddy-config:/config
  #   depends_on:
  #     - aeyez

volumes:
  aeyez-data:
  # caddy-data:
  # caddy-config:
